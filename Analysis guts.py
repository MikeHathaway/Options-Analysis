print ticker, filepath

with open(filepath) as handler, open('ticker.csv', 'rb') as csvfile:
	option_analyzer = csv.reader(csvfile)

def put_call_ratio(call, put, strike):
	for date in dates:
		date = 
			return put / call
		elif
	
